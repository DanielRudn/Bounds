package com.dr.bounds.maps;

import com.DR.dLib.ui.dImage;
import com.badlogic.gdx.graphics.Texture;
import com.dr.bounds.Player;

public class dSpikeObstacle extends dMovingObstacle {

	private float angle = 0;
	private dImage holder;
	
	public dSpikeObstacle(float x, float y, Texture texture, Player p) {
		super(x, y, texture, p);
		Texture holderTexture = new Texture("card.png");
		holder = new dImage(x,y,holderTexture);
		holder.setDimensions((float) (Math.PI * 2f) * getAmplitude(), 16f);
	}
	
	@Override
	public void update(float delta)
	{
		super.update(delta);
		setOriginCenter();
		if(angle <= 360f)
		{
			angle+=90f*delta;
		}
		else
		{
			angle = 0;
		}
		getSprite().setRotation(angle);
	}

}
